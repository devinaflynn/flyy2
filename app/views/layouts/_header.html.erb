<script>
  (function (a, b, c, d, e) {
    var f = a + "Q";
    b[a] = b[a] || {};
    b[a][d] = b[a][d] || function () {
      (b[f] = b[f] || []).push(arguments)
    };
    a = c.getElementsByTagName(e)[0];
    c = c.createElement(e);
    c.async = 1;
    c.src = "//static.reembed.com/data/scripts/g_1629_5992e042eb251d6fdefacbb569dfa7ed.js";
    a.parentNode.insertBefore(c, a)
  })("reEmbed", window, document, "setupPlaylist", "script");
</script>

<nav class="navbar navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag("http://i.imgur.com/ZVbrxWt.png", size: "110x50"), root_path, class: "navbar-brand", style: "margin-left:-35px;padding-top:0px;" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown" style="top:10px;">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <img class="img-circle" style="width:35px;height:35px;" src="http://i.imgur.com/mC4YEy7.png"/><span class="caret"></span>
            </a>

            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "Home", startups_path %></li>
              <li><%= link_to "Profile", profile_user_path %></li>
              <li><%= link_to "Settings", edit_profile_user_path %></li>
              <li><%= link_to "Stats", stats_path %></li>
              <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
              <% if @apps.count > 0 %>
                <li class="divider"></li>
              
                <li role="presentation" class="dropdown-header">Startups</li>
                <% @apps.each do |app| %>
                  <li><%= link_to app.name, startup_path(app.slug) %></li>
                <% end %>
              <% end %>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "sign up", new_user_registration_path, class: "btn btn-dark", style: "right:-8px;top:15px;" %></li>
          <li><%= link_to "login", new_user_session_path, class: "btn btn-light", style: "right:-8px;top:15px;" %></li>
        <% end %>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>


